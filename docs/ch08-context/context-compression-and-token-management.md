# 理论学习：上下文压缩与 Token 管理

> **架构定位**：本章实现的是记忆层的上下文管理组件。它让 Agent 能在有限的 Token 窗口内长时间工作。

---

## Agent 干了半小时活，怎么越来越慢了

你用 MewCode 跑了一个稍微复杂的任务，比如重构一个模块，Agent 先读了十几个文件了解代码结构，然后改了五六个文件，中间跑了好几次测试和编译，一切顺利。

但你慢慢会发现一个问题：越到后面，Agent 的响应越慢，而且 Token 用量蹭蹭往上涨，第一轮对话只用了 1000 多 input tokens，到第二十轮可能已经飙到十几万了。

![图片是一张折线图，展示了Agent对话轮次与输入Token数量的关系。横轴为对话轮次，从1到20；纵轴为Input Tokens，从0到200,000。图中折线从左下向右上逐渐上升，表明随着对话轮次增加，输入Token数量呈线性增长趋势。图中还有一条红色虚线，标注“200K上限”，表示Claude的上下文窗口上限为200K tokens。该图直观呈现了Agent对话过程中Token数量增长情况，与上下文提到的Token数量线性增长及上下文窗口上限问题相呼应。](https://feishu.cn/file/Stx1bs9lKorB6xxN7EdcBDu7nBh)

为什么？

因为 Claude API 是无状态的，每一轮请求，你都得把完整的对话历史发过去，Agent 读的每一个文件、执行的每一条命令、产生的每一段输出，全部堆在消息列表里，Token 数量随着对话轮次**线性增长**。

而 Claude 的上下文窗口有上限，200K tokens 听起来很多，但在 Agent 场景下真的不够用，一个 1000 行的源文件大约 15,000 tokens，读 10 个文件就是 15 万，再加上命令输出、搜索结果、AI 的回复，轻轻松松逼近 200K，一旦撞到上限，API 直接报错，Agent 就瘫了。

![图片以柱状图形式展示了一个200K Context Window的上下文管理情况。底部为System Prompt，中间有三轮操作，分别是第1轮ReadFile、第3轮Bash、第5轮EditFile，顶部有新请求。右侧有红色箭头指向顶部，标注“放不下了！”。该图与上下文紧密相关，直观呈现了在有限Token预算内，保留有价值信息、丢弃可牺牲信息的上下文管理问题，帮助理解Agent在处理大量信息时面临的困境。](https://feishu.cn/file/TwggbjUc2ovnlgxDqRCcFrnLnaf)

上下文管理要解决的核心问题就一个：在有限的 Token 预算内，保留最有价值的信息，丢掉可以牺牲的信息。

---

## Token 到底花在哪里了

在想解决方案之前，先搞清楚钱花在哪了。

你可能以为 Token 主要花在用户的问题和 AI 的回复上，但实际分布跟你的直觉差很远：

![图片展示了“一轮API请求的Token组成”，分为固定开销、用户+AI、文件内容、命令输出、搜索结果五个部分。其中固定开销占10%，用户+AI占5%，文件内容占55%，命令输出占20%，搜索结果占10%。工具结果（文件内容、命令输出、搜索结果）合计占85%左右。该图与上下文紧密相关，直观呈现了Token在不同消息类型中的分布情况，强调工具结果是Token消耗的绝对大头，占了85%左右，且工具结果的“保质期”较短。](https://feishu.cn/file/YmCBbFd7RoHJifx5ue6cBG99ncg)

| 消息类型 | 典型 Token 量 | 占比 |
|-|-|-|
| 固定开销（角色设定、环境信息、工具描述） | 5,000 - 15,000 | \~10% |
| 用户输入 | 50 - 500 | \~1% |
| AI 文字回复 | 200 - 2,000 | \~3% |
| 工具调用请求 | 50 - 200 | \~1% |
| 工具结果（文件内容） | 5,000 - 20,000 | \~55% |
| 工具结果（命令输出） | 500 - 5,000 | \~20% |
| 工具结果（搜索结果） | 500 - 3,000 | \~10% |

**工具结果是 Token 消耗的绝对大头**，占了 85% 左右，而且很多工具结果的「保质期」很短，Agent 在第 3 轮读了 `handler.py`，第 5 轮改了它，到第 10 轮，第 3 轮读的那个旧版本还占着 Token 预算，但内容已经过时了。

既然工具结果占比最高又最容易过时，压缩自然要从它们下手，用户的对话反而是需要着重保留的核心。

![图片展示了Agent在不同轮次对`handler.go`文件的处理情况。第3轮读取了`handler.go v1`，占用了15,000 tokens，但到第10轮时，第3轮读取的旧版本已过时，仍在占位。而第5轮已修改为`handler.go v2`，第10轮也已修改为`handler.go v2`。该图与上下文紧密相关，直观呈现了工具结果（文件内容）占Token消耗大头且易过时的问题，强调了Token管理中需压缩工具结果以释放资源。](https://feishu.cn/file/ZWNJb86XKolUthxDfw9cwJNenfh)

---

## 两层压缩：便宜的预防 + 贵但强的兜底

怎么解决？实际落地的策略可以归纳成**两层**：

| 层级 | 手段 | 信息损失 | API 开销 | 触发条件 |
|-|-|-|-|-|
| 第 1 层 | 大结果存磁盘 | 几乎为零 | 零 | 工具结果超阈值 |
| 第 2 层 | 摘要旧消息、保留近期原文（Auto-Compact） | 中 | 高（一次 API 调用） | token 数逼近窗口上限 |

第一层几乎不花钱也不丢信息，挡住的是单次工具输出过大的情况，第二层代价高但压缩效果强，是上下文快爆的时候的终极兜底，它并不是把整段对话都压成摘要，而是把较早的消息摘要掉、把近期原文留下来，所以信息损失只能算中等，不是把什么都抹平，两层之间的设计哲学就四个字：能轻则轻，轻量手段能扛住的，绝不动用昂贵的全量摘要。

![这张图对应文档中两层上下文压缩的策略说明，清晰呈现了理论中提到的两层压缩方案：第1层采用大结果存磁盘的手段，该层信息损失几乎为零，API开销为零，属于便宜的预防措施，用于应对工具结果超阈值的情况；通过标注“能轻则轻，逐步加码”的蓝色箭头，引出第2层的全量摘要，该层信息损失高、API开销高，属于贵但强的兜底措施，用于当token数逼近窗口上限时的场景，对应文档中“轻量手段能扛住的，绝不动用昂贵的全量摘要”的设计哲学。](https://feishu.cn/file/TPeJbiRYloLCN9xqq12cqdixnqf)

---

## 第 1 层：大结果存磁盘

这是最轻量的一层，几乎零信息损失。

它有一条贯穿始终的设计原则：**所有处理都发生在工具结果进入对话历史之前**，超限的内容在入口处就被换成预览，消息写进历史的那一刻就是最终形态，之后任何一轮都不再改动。

### 单个工具结果超限

当一个工具的执行结果超过 50,000 个字符（大约 12,500 tokens），系统不会把完整内容塞进对话历史，而是做两件事：把完整结果**写到磁盘上的一个文件**里，然后在对话历史里只放一个**预览加文件路径**。

![图片展示了工具输出的存储与消息优化流程。左侧呈现工具结果80KB，包含产品销售数据等信息，右侧有磁盘图标，表示完整内容写入磁盘。中间有“预览2KB + 文件路径”及“tool - results/”标识，表明仅在消息中保留预览和路径。右侧对话消息列表仅保留预览与路径，如用户请求分析2024年销售数据，系统回复预览及文件路径。该图与上下文紧密相关，直观说明了超限内容处理方式，即将完整内容写入磁盘，仅在消息中保留预览和路径，以减少消息体积，提升对话效率与性能。](https://feishu.cn/file/YexHb6fkvodZ1sxoFRfcQTu0nOh)

模型看到的是这样的：

```Plain Text
<persisted-output>
输出太大（80KB），完整内容已保存到：
.mewcode/sessions/{sessionID}/tool-results/toolu_abc123.txt

预览（前 2KB）：
=== 测试运行结果 ===
PASS: TestUserCreate (0.02s)
PASS: TestUserUpdate (0.01s)
FAIL: TestUserDelete (0.03s)
    expected: nil, got: permission denied
...
</persisted-output>
```

为什么这一层几乎零损失？因为完整结果还在磁盘上，如果模型后面需要看完整内容，用 ReadFile 读那个文件就行，而且这一层没有任何额外 API 开销，就是一次本地文件写入。

不过「用 ReadFile 读回」有一个闭环要处理：读回的内容和原始结果一样大，如果照常溢写，模型拿到的又是一个预览加路径，再读回又被溢写，永远看不到全文，所以溢写文件的读回结果会被直接放行，不做溢写。

实际使用中模型也很少需要一口气读回全文，预览里已经能看出结果的结构，用 ReadFile 的 offset 和 limit 参数按需读一段就够了。

### 每条消息的聚合限制

光管单个结果还不够，想象一下，一轮循环里 Agent 并行调了 10 个工具，每个结果都是 40K 字符，单个都没超 50K 单条阈值，但合起来就是 400K。

所以除了单个工具的阈值，还有一个**每条消息的聚合限制**：一轮并行调用的所有结果会进入同一条 user 消息，写入历史前先合计总量，超过 200,000 字符就把最大的那些存盘，直到总量降到预算以内。

比如五个工具返回 42K、38K、45K、40K、44K，合计 209K 超了 9K，挑最大的 45K 那条存盘替换成预览，其余四条保留原文，这条消息就此定型写入历史。

![图片展示了大结果存磁盘时每条消息聚合限制的示例。上方显示合计400K，10个工具结果各40K。下方聚合限制200K，6个工具结果各40K，4个已存盘，合计160K，符合聚合限制。说明超过聚合限制的最大结果集已被存盘，剩余结果的合计大小低于限制，聚合达标。该图与上下文紧密相关，直观呈现了每条消息聚合限制的计算与达标情况。](https://feishu.cn/file/CqbZbc8LMoRIrQxeMU0cqFGmnVc)

### 写入即终态与 Prompt Cache

替换之后对话里存的就是 preview，不再保留原文，原始内容只在磁盘存盘文件里，需要的时候模型用 ReadFile 读回来。

这种做法对 Prompt Cache 天然友好，Prompt Cache 的工作原理是如果这一轮请求的消息列表前缀和上一轮完全一样（逐字节一致），API 可以跳过前缀部分的处理只处理新增的消息，而历史里的每条消息从写入起就不再变化，每一轮请求里老消息都和上一轮逐字节相同，前缀稳定不需要任何机制来专门维护，它是「写入即终态」的副产品。

这也是上下文管理里一个容易被忽视的设计约束：不仅要考虑「保留什么信息」，还要考虑「改动对话前缀的代价」，如果每轮都重新评估、重新改写历史里的老消息，prompt cache 命中率会直接归零，每轮请求都在为重复处理前缀买单。

---

## 第 2 层：摘要旧消息、保留近期原文（Auto-Compact）

当第一层不够用了，上下文仍然接近爆炸，就要动用最后的手段：**调用 LLM 自己来生成一份对话摘要**，把较早的那些消息换成摘要，同时把近期原文留下来。

这是代价最高的一层，需要额外调一次 API，花钱，而且被摘要掉的部分不可能保留所有细节，信息丢失不可避免，但它也是压缩效果最强的一层，能把十几万 Token 的早期对话压缩到几千 Token，再配上保留的近期原文，模型既不会撑爆窗口，又能看清最近发生了什么。

### 什么时候触发

以 200K Token 的上下文窗口为例，阈值是这么算的：

```Plain Text
上下文窗口               200,000
 - 预留给摘要输出         - 20,000    摘要本身也要占空间
= 有效窗口              180,000
 - 安全余量              - 13,000    防止 Token 估算误差导致临界抖动
= 自动压缩阈值          167,000     超过这个数就触发全量摘要


```

为什么要留 13,000 的安全余量？自动压缩的检查点是每轮循环开始时、调 API 之前，检查通过后，模型回复、执行工具、产生新的 tool result，这些新增内容在下一次检查之前是没有被覆盖到的，而且模型可以在一次回复中发起多个并行工具调用，一轮下来可能同时新增好几个 tool result，13,000 的 buffer 就是给这个「两次检查之间的盲区」留的余量。  
  
13K 是一个经验值，不是精确推导出来的，你自己做 Agent 时可以根据实际场景设一个合适的数，它也不需要精确，因为即使超了也不会崩，后面「紧急压缩」会兜底：API 返回 prompt_too_long 时，Agent 自动触发一次压缩再重试，不会直接瘫掉。

这里还有一个细节，关系到「已用了多少 token」这个数准不准，MewCode 不是把整段对话拿字符数粗估一遍，那样误差会随对话变长越滚越大，它锚定的是上一次 API 返回的真实用量，只对这之后新增的一小段消息做字符估算，整段历史的绝大部分是精确值，误差被压缩在最后一小段增量上，13,000 的余量刚好够覆盖它。

![图片展示了上下文窗口的计算流程。初始上下文窗口为200,000，减去摘要输出预留的20,000后，有效窗口为180,000。再减去安全余量13,000，得到自动压缩阈值167,000。图片右下角标注“防止估算误差导致临界抖动”，并标明“by 公众号@小林coding”。该图与文档中上下文压缩与Token管理的理论学习内容相关，直观呈现了计算过程。](https://feishu.cn/file/D2pNblI1jocf0DxeVFgct3MOnHg)

### 为什么是固定值而不是百分比

你可能会问：为什么不简单地设成「上下文用到 X% 就触发」？

因为 buffer 保护的是「单轮波动」，不是「总量」，每一轮 Agent 循环新增的 token 量是相对固定的：读一个文件 \~15K token，跑一条命令几百到几千 token，不管你的窗口是 200K 还是 1M，一次 ReadFile 都是那么大，buffer 要防的风险是「下一轮突然多了一个大工具结果」，这个风险的大小取决于单次工具结果有多大，跟总窗口无关。

如果用百分比会怎样？

| 方案 | 200K 窗口 | 1M 窗口 |
|-|-|-|
| 90% 触发 | 剩 20K，刚好够一次大文件 | 剩 **100K**，**浪费了**，能装 7 个大文件 |
| 98% 触发 | 剩 **4K**，**太危险**，一次普通文件读取就撞墙 | 剩 20K，刚好 |

调低了在大窗口浪费空间，调高了在小窗口撞墙，**不存在一个百分比能同时适配所有窗口大小，**而固定值没有这个问题：33K 的 buffer（20K + 13K）在任何窗口大小下都恰好够「挡住一到两次大工具调用」。

用同一套公式算 1M 窗口的阈值：

```Plain Text
上下文窗口             1,000,000
 - 预留给摘要输出       -  20,000
= 有效窗口              980,000
 - 安全余量             -  13,000
= 自动压缩阈值          967,000


```

1M 窗口下 Agent 可以用到 967K 才触发压缩，充分利用了大窗口的容量，而 buffer 仍然是 33K，刚好够应对单轮波动。

### 为什么预留 20K 给摘要输出

这个 20,000 是被两个约束从两头夹出来的。

下限来自摘要本身的需求，摘要有 9 个结构化部分（后面会详细讲），加上 `<analysis>` 草稿块，一次复杂会话的摘要输出大约 15,000 到 18,000 tokens，设成 15K 就有被截断的风险，20K 刚好够用。

上限来自有效窗口的压力，预留越大，有效窗口越小，压缩就触发得越频繁，如果预留 50K，有效窗口缩到 150K，自动压缩在 137K 就触发了，但摘要根本用不了 50K，模型不会因为你给了更多输出空间就写更长的摘要，摘要长度取决于对话的复杂度。多出来的 30K 预留纯粹是浪费。



### 摘要 Prompt 的设计

摘要质量直接决定了压缩后 Agent 的表现。一个好的摘要 Prompt 要求 LLM 生成一份**结构化摘要**，明确指定 9 个部分：

```Plain Text
1. 主要请求和意图        用户到底想做什么
2. 关键技术概念          讨论过的重要技术点
3. 文件和代码段          涉及哪些文件，关键代码片段要保留
4. 错误和修复            遇到了什么错，怎么解决的
5. 问题解决过程          解决问题的思路和方法
6. 所有用户消息          用户说过的所有非工具结果的话（原文保留！）
7. 待办任务              还没完成的事
8. 当前工作              最近在做什么（要最详细）
9. 可能的下一步          接下来打算做什么


```

注意第 6 条：**用户说过的话尽量**原文保留，为什么不能摘要改写？因为用户的原始表述包含意图、偏好、语气，如果用户说「不要用 interface{}，用 any」，你把它摘要成「用户偏好现代语法」，模型下次可能给出 `type any = interface{}` 这种不是用户想要的东西。

原文才能准确传达意图，当然，如果对话很长、用户消息很多，LLM 不可能在有限的摘要空间里塞进所有原文，这更多是一个**优先级指引**：告诉 LLM 用户消息优先保留原文而不是随意改写，空间不够时自然会取舍。

![图片展示了结构化摘要的9个部分，分别为：①主要请求、②关键概念、③文件代码、④错误修复、⑤解决过程、⑥用户原话（原文保留·不可改写）、⑦待办任务、⑧当前工作、⑨下一步。其中，用户原话部分以黄色框突出显示，强调原文保留、不可改写。该图片与文档中“保留近期原文（Auto-Compact）”的内容相关，直观呈现了结构化摘要的构成，帮助理解用户原话在其中的重要性。](https://feishu.cn/file/Ze6ZbFk0codcs2xnje3cXaGQnuX)

### 两阶段生成：先打草稿再写正文

还有一个很聪明的设计：Prompt 要求 LLM 先产出一个 `<analysis>` 草稿块来梳理思路然后产出正式的 `<summary>` 块，最终**只保留 summary，analysis 被丢弃**。

为什么要「先打草稿再写正文」？因为实验发现这能显著提升摘要质量，分析阶段让 LLM 先把对话中发生了什么梳理一遍，然后在此基础上写摘要会更全面、更准确，但 analysis 本身作为中间产物没有保留价值，占了空间反而浪费，所以生成完就扔掉。

![图片展示了两阶段生成的思维导图。左侧“analysis 草稿”部分，主题为社交媒体对学习的影响，列出好处、坏处及结论等内容，还提示待补充。右侧“summary 正稿”部分，总结社交媒体对学习的积极与消极影响，结论强调合理使用、自律。草稿用完即弃，正稿最终保留。该图与文档中“两阶段生成：先打草稿再写正文”的内容相呼应，直观呈现了这一生成流程。](https://feishu.cn/file/SQvmbowr8oeOfuxfkiecSWeSnic)

### 禁止工具调用

Prompt 还需要一段强硬的开头，明确禁止模型调用任何工具、只输出纯文本，为什么需要这条禁令？因为摘要 API 调用复用了原对话的消息上下文，工具定义会随消息一起传入这次调用，模型仍然「看得到」可用工具，如果不明确禁止，LLM 可能「好心」地试图调一个工具来帮你，结果浪费了这次调用，

MewCode 调摘要 API 时会保留 `tools` 参数，这样做是为了让压缩请求的前缀和正常请求完全一致，命中 Prompt Cache，为了防止模型在压缩时调用工具，除了 prompt 里两头堵的禁令外，压缩代码只读取文本输出，工具调用事件直接忽略。

实践中，这段禁令往往出现两次：开头一次，结尾再强调一次，开头说「你只能输出纯文本」，结尾说「提醒：不要调任何工具，工具调用会被拒绝，你会失败」，两头堵，防止模型在长上下文中「忘记」开头的指令。

### 压缩后恢复

第 2 层并不是把整段对话都换成摘要，它的做法是把较早的消息摘要掉，同时**保留近期原文**：从尾部按 token 往回数，大约最近 1 万 token 或者至少 5 条消息留作原文，且不会从中间切断 tool_use 和 tool_result 的配对，这部分近期原文一字不改地留在对话里，模型最近做了什么、刚刚说了什么，看的都是原话而不是摘要。

在保留的近期原文之外，还有一些被摘要掉的关键上下文需要**重新附加**回来：

- **最近访问的文件**：最多恢复 5 个，每个最多 5,000 Token。Agent 压缩后仍然「记得」最近读过的文件
- **技能定义**：如果之前使用过 Skill，重新注入定义，总预算 25,000 Token

压缩后的消息列表看起来像这样：

````Plain Text
[system]    你是一个编程助手...
[user]      本次会话延续自之前的对话，因上下文空间不足进行了压缩。
            以下是早期对话的摘要：
            用户要求重构 auth 模块。读取了 handler.py、
            middleware.py、router.py，修复了第 42 行错误处理 bug，
            测试全部通过。待办：还需要更新 API 文档...
            近期消息已原样保留。
            如果你需要压缩前的具体细节（代码片段、报错信息等），
            请用 ReadFile 读取完整会话记录：
            /home/user/project/.mewcode/sessions/20250601-143022-a1b2.jsonl
            ---
            ## 最近访问的文件
            以下快照是文件读取工具上次返回的内容。
            如需最新内容，请用工具重新打开。

            ### handler.py（读取于 2025-06-01T14:30:05Z）
            ```
            def handle_request(req):
                validate(req)
                ...
            ```

            ### middleware.py（读取于 2025-06-01T14:28:12Z）
            ```
            class AuthMiddleware:
                ...
            ```

            ## 已激活的技能
            ### code-review
            审查代码变更，关注正确性和安全性...

            ## 提示
            以上分隔线之前的内容均为重建的上下文。
            如需精确的代码、报错信息或用户原文，
            请重新读取源文件，不要凭摘要猜测。
[user]      好，接下来帮我更新 API 文档     ← 保留的近期原文
[assistant] 我来看一下当前的文档结构...
...
````

注意看这个结构：摘要、会话记录路径、恢复的文件快照，全部拼在**同一条 user 消息**里，用 --- 分隔，恢复文件不是单独的消息，而是作为附件拼接在摘要后面，这条消息之后紧接着的是保留的近期原文（也是 user），两条连续的 user 消息会在发送给 API 之前被序列化层（buildAnthropicMessages）自动合并成一条，压缩模块不需要操心交替规则。

会话记录路径的设计防止模型「编造」摘要里没有提到的细节：摘要里写「修复了 handler.py」，模型如果想引用具体代码，就得去读会话记录或者重新 ReadFile，而不是根据模糊描述脑补一段不存在的代码。

![图片展示了Agent压缩后仍记得最近读过的文件的对话示例。左侧为压缩后的对话历史，包含用户要求重构auth模块、需重新ReadFile、恢复文件handler.go和middleware.go等信息。右侧是最近5条完整保留的对话，用户请求将登录逻辑从handler中抽离到service层，助手回应并更新了middleware.go，最后重构完成，通过所有测试。该图与上下文紧密相关，直观呈现了技能定义中Agent压缩后仍记得最近读过的文件这一特性。](https://feishu.cn/file/DGNdbZ3cNotEz0x1YO8cgT1an1d)

### 熔断机制

如果全量摘要因为网络问题、API 错误或 prompt-too-long 等原因连续失败 3 次，系统**停止自动触发**。

为什么？想象一下：上下文已经超限了，每一轮都触发摘要，每一轮都失败，然后每一轮又重试...Agent 被卡在死循环里，熔断打破这个循环：3 次失败后认输，让用户手动处理。

![这张图以熔断装置为核心视觉载体，搭配三个呈现不同状态的红色灯泡，对应熔断机制中的失败场景：前两个灯泡状态正常显示“失败1”“失败2”，第三个灯泡破碎冒烟标注“失败3”，直观展现熔断触发的结果。图中明确标注“停止自动触发”，旁侧配有“交给用户手动处理”的提示，对应上下文提到的熔断机制作用——防止因摘要请求过大导致的死循环，触发后系统暂停自动压缩，需转而由人工介入处理后续流程。](https://feishu.cn/file/TaShb2lCKoc4zBxw59acRLEHngf)

还有一种极端情况：**摘要请求本身太大了**。要摘要的内容太多，超出了 API 限制，可以用这样一套重试逻辑来应对：

```Plain Text
如果摘要请求报 Prompt Too Long：
  1. 把消息按 API 轮次分组
  2. 丢弃最旧的几组
  3. 用剩余消息重试
  4. 最多重试 3 次
  5. 还不行就丢掉 20% 的消息组再试


```

### 强制压缩线：熔断之后的保底

熔断虽然防止了死循环，但带来一个新问题：自动压缩停了，token 还在涨，上下文会越来越逼近窗口上限。

为了应对这种情况，系统在 effectiveWindow - 3,000 的位置（以 200K 窗口为例就是 177K）设了一条强制压缩线，每轮循环检查 token 用量时，如果已经越过了 177K，不管熔断状态如何直接执行一次 `ForceCompact`，这条检查在熔断判断之前，优先级最高。

### 紧急压缩：撞墙后的自救

上面讲的都是调 API 之前的预防措施，但还有一条路径需要处理：预防措施全都过了，**正常的对话请求**发出去后，API 返回 `prompt_too_long` 错误。

这说明所有阈值都没挡住，可能是 token 估算偏差太大，也可能是某一轮工具结果异常巨大，两次检查之间直接跳过了阈值，不管什么原因，结果就是上下文已经超了 API 的硬限制，请求被拒绝了。

如果没有任何处理，Agent 就直接报错停工了，用户只能手动 `/compact` 然后重试，体验很差，

MewCode 的做法是在 Agent Loop 里捕获这个错误，立刻触发一次 `ForceCompact`，压缩完成后用新的消息列表**重试原来**的请求，相当于「撞墙了，赶紧压缩，然后再试一次」，如果压缩后仍然超限，就按正常错误流程处理，不再无限重试。

到这里整套防线就完整了：token 用量到 167K 触发自动压缩，连续失败 3 次后熔断停止重试，涨到 177K 时强制压缩兜底，这三道都是调 API **之前**的预防，如果还是漏过去了、请求被拒，紧急压缩在调 API **之后**补救，压缩完成重试原请求，Agent 不至于因为一次估算偏差就彻底瘫痪。

---

## 手动 /compact

除了自动压缩，用户随时可以输入 `/compact` 手动触发。两个典型场景：

**预防性压缩**：你知道接下来要读一大堆文件，提前压缩腾出空间。

**话题切换**：前面的对话在调 bug A，现在要处理 bug B。手动压缩清理掉 bug A 的细节，减少噪声，

![图片展示了手动/compact的两个典型场景。左侧为预防性压缩，画面中人物手持“/compact”按钮，心想“接下来要读一大堆文件”，背景有大量文件图标。右侧为话题切换，上方显示“Bug A”，下方有对话框，人物心想“/compact”，画面下方有“Bug B”字样，底部有“/compact”按钮，背景有对话框和机器人图标。图片直观呈现了手动压缩在不同场景下的应用，与上下文对手动/compact场景的描述相契合。](https://feishu.cn/file/YLIQbZgOqomLXwx2MokcAia8nQb)

手动压缩和自动压缩走的是同一套压缩逻辑，区别只在触发方式：自动是系统检测到阈值后触发，手动是用户主动敲命令触发。

---

## 两层的协作关系

最后整理一下两层之间的关系。

两层管的是不同维度，第 1 层管**入口**，工具结果进入历史前把单条消息的体积压在预算内，防止一次超大输出直接把上下文撑爆，是一个**预防性**措施，第 2 层管**累积**，每轮调 API 前检查整段历史的 token 总量，防止几十轮对话下来总量超限，是一个**治疗性**措施，进入历史的消息都是第 1 层处理过的最终形态，第 2 层估算 token 时拿到的就是真实大小。

一个常见的运行场景：Agent 连续读了 20 个小文件，每个都没超 50K 阈值，第 1 层什么都没做，但累积下来总 Token 数逼近 167K，第 2 层触发，把较早的对话摘要掉、保留近期原文，整段历史一下子缩到几千 Token 的摘要加上一小段近期原文。

另一个场景：Agent 执行了一条命令，输出了 80K 字符的日志，这超 50K 单条阈值，第 1 层把它存盘，只留预览，上下文总量远没到阈值，第 2 层不触发。

![图片展示了工具结果进入历史前及每轮调API前的处理流程。工具执行返回结果先存盘，若超限则写入对话历史；第1层管入口单条大小，第2层管累积长度，检查token总量，若逼近167K则摘要旧消息、保留近期原文，治疗一次API调用后发送请求。该图与上下文紧密相关，直观呈现了上下文压缩与Token管理的处理步骤，帮助理解Agent在不同场景下的操作逻辑。](https://feishu.cn/file/KN4Pbk4DRoMBMoxgSzbcQEHanJg)

---

## 本章小结

上下文管理是 Agent 从「短任务玩具」变成「长时间工作伙伴」的关键。没有它，Agent 跑几轮就撞墙，

两层压缩体现了一个简洁但有效的设计哲学：能轻则轻，大结果存磁盘几乎零损失零成本，只有当对话累积到临界点时才动用全量摘要这个昂贵但强力的手段。

真实的生产级 Agent 可能还会在两层之间加一些中间手段，比如 Claude Code 会按时间或数量清理过期的工具结果（microcompact），在不触发全量摘要的前提下释放空间，但核心骨架就是这两层，对于我们自己做 Agent 来说，先用存盘挡住大结果，再用摘要兜底长对话，就足够撑起一个能长时间工作的 Agent 了。
