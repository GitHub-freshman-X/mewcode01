# Project Instructions

## 项目文档优先

- 当用户询问当前项目相关的问题时，优先检查 `docs/` 目录中是否存在对应章节、设计文档、规格、计划、任务或验收清单。
- 对项目目标、架构设计、功能边界、技术取舍和验收标准的回答，应先以 `docs/` 中的相关文档建立上下文，再按需阅读代码。
- 涉及当前实际实现、运行行为、bug、测试结果或文档可能已经过时的内容时，查阅文档后仍需核对对应代码和测试；代码与实际验证结果是当前行为的最终依据。
- 如果文档与代码不一致，应明确指出差异，不得只依据其中一方推断另一方已经同步。
- 仅在 `docs/` 中没有相关信息，或问题本身要求具体实现细节时，才直接从代码开始调查。

## 文档面向用户

- `docs/` 下每章的 Spec、Plan、Tasks、Checklist 和人工测试方案都是用户复习项目时阅读的正式文档，不是仅供 Agent 使用的临时产物。
- 修复某章节的 bug 或扩展既有章节功能时，优先回写该章节已有文档；除非用户明确要求，不得为同一章节新增并行的“修复版”“补丁版”或仅面向 Agent 的文档。
- 对既有章节文档的更新应保持可读、连贯并反映当前行为；若文档与实现仍有差异，必须明确标注。

## 章节开发分支策略

- 每次开始新一章需求开发时，应先从当前稳定的 `main` 分支创建新的章节开发分支，避免直接在 `main` 上落地大范围改动。
- 分支名称应能体现章节和主题，例如 `ch05-system-prompt` 或在工具要求下使用带前缀的等价名称。
- 章节分支内完成实现、文档更新、bug 记录同步和自动化验证；确认该分支可以正常构建、测试和运行后，再合并回 `main`。
- 章节开发必须在新增关键流程、状态迁移、失败分支和外部边界处接入既有 `internal/logging` 模块；日志字段应只包含阶段、状态、计数、类型、耗时、大小、Token 等安全元数据，不得记录 prompt、工具结果正文、密钥、请求头或未经脱敏的错误载荷。
- 当章节开发新增、修改或移除任何可配置项时，必须同步更新 `.mewcode/config.example.yaml`；该示例文件应记录所有可设置配置项，有默认值的配置项应填写实际默认值，让用户能直观看到完整配置面。
- 合并回 `main` 前，应明确记录已通过的验证命令和仍未验证的事项；不得在验证失败或状态不明时合并。
- 如需保留每章完成时的可运行快照，优先在合并点创建 tag，而不是长期保留多个未合并章节分支。

## Bug 记录同步

- 当任务的主要目的为排查、修复或验证 bug 时，每次执行相关命令或修改代码后，都必须同步检查并更新 `bugs/` 目录中的记录。
- 无论 bug 是否成功修复，都必须更新记录，不得因为修复失败或任务中断而省略。
- 已修复的问题应记录修复方案、验证方式和最终状态。
- 尚未修复、仅部分修复或无法验证的问题应明确标记为“待处理”，并记录当前进展、失败原因、阻塞条件和建议的下一步。
- 新发现且具有实际诊断价值的问题，应按照 `bugs/README.md` 的目录与编号约定创建记录。
- 不属于 bug 的既定功能边界、明确设计行为或单纯功能增强，不强制记录为 bug。
